
# Metadata about the indexer
type DBMetaInfo struct {
    Version Int
    Format  String
}

# Metadata about index
type IndexMetaInfo struct {
    Version Int
}

# Metadata about collection
type CollectionMetaInfo struct {
    Version Int
    PrimaryKey optional [String]
}